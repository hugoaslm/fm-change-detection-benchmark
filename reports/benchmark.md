# LEVIR-CD Zero-Shot Change Detection Benchmark

No LEVIR-CD results have been generated yet.

Run:

```bash
uv run fmcd benchmark \
  --config configs/baseline.yaml \
  --data-root /path/to/LEVIR-CD
```

The command regenerates this file from real result records. Synthetic smoke metrics must not
be presented as benchmark findings.
